# deps / build
node_modules/
dist/
*.tsbuildinfo

# env / secrets
.env
.env.*
!.env.example

# data / logs
data/
*.log

# claude local
.claude/settings.local.json

# os
.DS_Store

# 机器生成的降噪 overlay（apply-feedback 写、loader 读）：属运行期状态，不进版本库。
# 它里面是真实发件人地址，误提交等于把 PII 推进仓库；而部署时一个 git clean 会抹掉 operator 攒的整份名单。
rules/noise_senders.overlay*
