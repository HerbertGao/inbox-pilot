## 1. 依赖与配置 + 凭据约束改述

- [x] 1.1 `pnpm add googleapis google-auth-library`；确认锁文件与 `pnpm install` 通过
- [x] 1.2 `src/config/config.ts`：新增 `GMAIL_CLIENT_ID` / `GMAIL_CLIENT_SECRET` / `GMAIL_REDIRECT_URI`（app 凭据，仍从 env、可选、空串归一；`GMAIL_REDIRECT_URI` 约定 host/path、端口运行时绑定填入；**校验 host == `127.0.0.1` 且 path == `/oauth2/callback`、剥离/忽略 env 中任何端口**（端口始终用运行时绑定值；监听亦绑 127.0.0.1；**不允许 `localhost`/`[::1]`** 以避免 IPv4/IPv6 与解析歧义致授权静默失败），否则 Gmail onboarding 显式失败）；**移除/弃用** env 单 IMAP 读取（`IMAP_*` 不再作账号凭据来源——保留一轮供迁移脚本读取，迁移后删）
- [x] 1.3 `src/logger.ts` redact：**主控整体 redact `authJson`/`*.authJson`**；叶子 `password`/`refreshToken`/`accessToken` + **snake_case** `refresh_token`/`access_token`/`client_secret`；**运行期 OAuth2 client 凭据(不在 authJson 内)单独枚举真实键路径**：整体 redact `credentials`/`*.credentials`、`tokens`/`*.tokens` 子树 + `_clientSecret`/`*._clientSecret` + PKCE `codeVerifier`/`code_verifier` + `authorization`/`*.authorization`（小写,防 headers 对象直接入日志泄露 bearer——主防仍是「不记原始 error」）+ 数组路径（**redact 路径须与声称覆盖一一对应、不留缺口**）；注释加「禁打印账号/注册表对象整体、OAuth2 client 对象、token 响应、原始 Prisma/OAuth/IMAP 错误对象**及其 `cause` 链/`AggregateError.errors`**（只记 `code`/脱敏 message）」
- [x] 1.4 改述硬约束：CLAUDE.md + openspec/config.yaml 把「密钥只从环境变量读」改为「app 凭据从 env；账号凭据存 DB `MailAccount.authJson`、整体绝不入日志」；**修正 `PROJECT_INIT.md` 与 `.env.example` 两个文件的旧 `GMAIL_REDIRECT_URI`**（`http://localhost:3000/oauth/gmail/callback` 固定端口/localhost/旧 path → `http://127.0.0.1/oauth2/callback`，端口运行时绑定、Desktop client 无需固定）
- [x] 1.5 config 单测：GMAIL_* 可选解析；非 loopback `GMAIL_REDIRECT_URI` → onboarding 失败；缺 GMAIL_* → Gmail onboarding 不可用但服务正常

## 2. 账号注册表 + Provider 抽象

- [x] 2.1 `src/providers/provider.ts`：定义 **poller**（`poll()`）+ 扩展 **`ProviderActions`**（新增 `reflectPriority(email, decision)`，保留 `markRead(email)`）；定义**带类型致命错误 `ProviderReauthRequired`**（账号级、需重授权，区别于发送态瞬时失败；**只携 `{accountId, kind}`，绝不挂原始 GaxiosError 作 `cause`**——防 cause 链泄露 token）；**`FakeProviderActions` 同步加 `reflectPriority`**（否则现有测试不编译）；**`ImapAccount`（连接参数 `{accountId,host,port,user,password,tls}`）从 `accountService` 迁来此处/registry**（`accountService` 的 env-single 被移除，`createRealImapConnection`/`pollAccount` 仍消费同形状）；`Account` 凭据字段距被记录对象 ≤1 层、不整体记录；IMAP ProviderActions 由 `pollOnce` 本轮连接构造
- [x] 2.2 `src/accounts/accountRegistry.ts`：`loadEnabledAccounts(repo)` 读 enabled 行 → 解 authJson（imap 映射为 `ImapAccount` 形状）；`accountId = MailAccount.id`；`provider ∉ {imap,gmail}` 跳过+记错；**imap 缺 host/user/password、gmail 缺 refreshToken、authJson 非预期对象** 各跳过+记错（**跳过日志只记 `{accountId, provider, reason-kind}`,绝不记 authJson 值或 `JSON.parse` 错误对象**——其 `.message` 回显含凭据的输入子串）；无账号返回 []
- [x] 2.3 把 P3 的 `imapClient`/`imapPoller`/`imapActions` 收敛实现 poller + ProviderActions（imap：`reflectPriority` no-op；`markRead` 加 `\Seen` 走本轮连接）；移除 env 单账号 `loadImapAccount`；**移除/no-op `ensureAccountAnchor`/`buildAnchorUpsertArgs`/`AnchorAccount`/`AnchorUpsertArgs` 及其 call-shape 测试**；**更新 `mailRepo.ts` 模块/类型 JSDoc** 由「env 是真相源、DB 仅锚定」改为「`MailAccount` 是唯一真相源」
- [x] 2.4 扩展 `MailRepo`/`PrismaMailRepo`/`InMemoryMailRepo`：`listEnabledAccounts()` + **显式 `createAccount`/`upsertAccount` 写入路径**（CLI add/迁移/测试共用——取代被移除的 `ensureAccountAnchor`；`InMemoryMailRepo`「未播种 accountId `setCursor` 抛错」契约改键于此）+ **`setAccountEnabled(id, false)`**（reauth-suspend 持久化用，复用 `enabled` 字段、无 schema 迁移）；**id**：gmail `gmail:<getProfile 邮箱仅小写>`、imap `--account-id`‖确定性 `imap:<user>@<host>` 以**主键 upsert**（不模糊查既有行）；写 `MailAccount.id` 显式（覆盖 cuid）、`email` 非空
- [x] 2.5 单测：注册表加载混合、非法 provider 跳过、imap/gmail 凭据不全各跳过、无账号→[]；**redact 断言覆盖嵌套深度 + snake_case + 数组 + `error.cause` 链 + `client_secret` + `credentials`/`tokens`/`code_verifier`；整体记录 account/`NormalizedEmail` 对象不泄露 to/cc/凭据;authJson 解析失败日志不含 authJson 值**；CLI add 行 `id`==派生/`--account-id`、`email` 非空；同邮箱 upsert 命中同一行不分裂

## 3. executeActions 扩展：reflectPriority + 致命错误通道

- [x] 3.1 `src/actions/actionTypes.ts` 新增 `ActionType` **`reflect_priority`**；`executeActions.ts` 动作**顺序 `reflectPriority` → `markRead`(仅 shouldMarkRead) → `notify`(仅 shouldNotifyNow) → `markProcessed`(最后)**；**两类错误通道**：发送态瞬时失败(含 429)落 `mail_actions`、**不抛**、不阻断后续与 markProcessed；但 **`ProviderReauthRequired`（致命）必须重新抛出**(不当发送态吞)——动作 helper 须**在其自身 `recordAction→updateAction` 作用域内**捕获 `ProviderReauthRequired`、`updateAction(rowId,'failed',reauthKind)`（rowId 在作用域内）后再 re-throw,**不留 orphan pending 行**(测试断言 re-throw 后该行为 `failed`、非 `pending`)——→ 由 `processEmail` 传播跳过 `markProcessed`；只认 `FinalDecision`、不读 Classification；`reflectPriority` 幂等
- [x] 3.2 单测：P0/P4 → reflectPriority 调、markRead 不调；P2/P3 → 顺序 reflectPriority→markRead→（notify）→；reflectPriority 发送态失败不阻断 markRead；**`ProviderReauthRequired` 重抛 → markProcessed 不调用**；终态落库失败向上传播；**更新所有现有 executeActions/processEmail 测试加 reflectPriority 桩**

## 4. Gmail provider

- [x] 4.1 `src/providers/gmail/oauth.ts`：authorize URL 带 **`access_type=offline`+`prompt=consent`+PKCE(S256, `generateCodeVerifierAsync`)+`state`**；**loopback 临时端口监听(仅绑 127.0.0.1)**：**路径精确匹配**、`state` **先长度检查再常量时间比较**(缺失/不等长按非匹配、不抛)、**不匹配不消费 one-shot**、匹配则 state/code 只消费一次、`error=`(access_denied)干净中止、`finally` 关监听 + 同意超时(与单次消费互斥) + **限请求行/头大小+连接数**；**回调处理只记 `{kind,state-result,path}`、绝不记 `code`/`state`/完整回调 URL**；verifier 回传 `getToken`；**缺 refresh token 显式失败**；email 仅取 `getProfile().emailAddress` **仅小写**(不剥点/+tag)；scope **恰等 `{https://www.googleapis.com/auth/gmail.modify}`**；**全代码无 `messages.send` 路径**
- [x] 4.2 `src/providers/gmail/gmailClient.ts`：封装 gmail client（按 refresh token 构造、自动 refresh）；**refresh 后只回写 refreshToken(若轮换)、绝不写 `access_token`/`expiry` 入 authJson**（回写持久化失败只记 `code`+kind，**禁记原始 Prisma 错误**——其 params 可能含轮换的 refresh_token）；refresh 失败只记 `error.code`+固定 kind（禁记 GaxiosError `.response`/`.config`/cause），**抛 `ProviderReauthRequired`（与 403 同类型，使 guard suspend+`enabled=false`——console 撤销/过期 token 也跨重启停轮询，不只本轮 isolate）**
- [x] 4.3 `src/providers/gmail/gmailPoller.ts`：`messages.list(q='is:unread')` **每轮穷尽 `nextPageToken` 翻页(列完所有未读页、纯 id 轻)** → 每页**先用 message id 对 `(accountId,providerMessageId)`+`processedAt` 预去重(命中跳过、不 get)** → 未处理者**按最旧优先(list 默认最新优先故逆序)** `messages.get(format='full')` **至 get 预算(预算在 get 数而非翻页数,`// ponytail: 默认 ≤200/轮`;超出者下轮穷尽 list 再处理、DB processedAt 即持久进度、无 Gmail 游标;最旧优先防新件持续涌入饿死旧件)** → 映射（message id/threadId、snippet、`text/plain`→textBody；**无 text/plain 则 `text/html` 先剔除 `<script>`/`<style>` 块再去标签→textBody（防 CSS/JS 文本污染分类输入）**；htmlBody 可选；**`payload.headers` 仅取分类器白名单 `reply-to`/`return-path`/`list-unsubscribe`/`authentication-results`→headers**；**全无正文→subject+headers 分类**）→ `normalizeEmail` → `processEmail`；**映射/解析错误逐封 skip（catch 只记 code+kind+accountId+providerMessageId,不记原始 GaxiosError / 不整体记 `NormalizedEmail` 对象——5xx 的 `.config.headers.Authorization` 含 bearer、`to`/`cc` 是第三方 PII）**，但**429/配额 与 `ProviderReauthRequired`(403) 绕过逐封 skip、上抛结束本轮并隔离**；不调 get/setCursor
- [x] 4.4 `src/providers/gmail/gmailActions.ts` 实现 ProviderActions：`markRead`(removeLabelIds `['UNREAD']`，幂等)+ `reflectPriority`(**权威标签** P0→`AI/P0_Important_Now`…P4→`AI/P4_Risk`；`AI/Processed` 可选 cosmetic；标签缺失 `labels.create`，**labelId 按 `(accountId,labelName)` 缓存**，**409→取该账号已存在 labelId**)；**写侧 429 非致命**(脱敏、落 failed、不抛)；**403 权限不足/scope 漂移 → 抛 `ProviderReauthRequired`**(致命、由 executeActions 重抛隔离账号)
- [x] 4.5 单测（注入假 gmail client）：未处理未读进流水线；**已处理长期未读经 DB 预去重不再 get**；**分页翻页不饿死旧未读**；reflectPriority 打权威标签(缺失→create、409→取已存在、缓存按账号隔离)；`shouldMarkRead`→去 UNREAD、P0/P4/敏感保持 UNREAD+带标签；**reflectPriority 先于 markRead**；**HTML-only 去标签→textBody 喂分类器**；全无正文 subject/headers 分类；单封缺 From skip；**读侧 429 结束本轮（不逐封 skip 继续翻页）**；写侧 429 非致命仍 markProcessed；**403 抛 `ProviderReauthRequired`→ markProcessed 跳过、账号隔离标重授权**；**access_token 不落 authJson**；**scope 恰为 `{gmail.modify}` 精确相等 + 断言构建 provider 方法面无 send(结构性) + PKCE verifier 全程一致**；refresh 失败日志无 token/secret 子串(合成 GaxiosError 的 `.message`/`.response`/`.config` 都含凭据子串,断言均被丢弃、只留 `error.code`+kind)；**回调处理日志无授权 `code`/`state` 子串、不含完整回调 URL**

## 5. per-account 调度 + main 接线

- [x] 5.1 `src/jobs/scheduler.ts`：per-account——每账号一把 `isPolling` 锁；**单账号异常 catch+记错(只 code+kind、不记传播对象/cause)不拖累其他**；**仅当错误是 `ProviderReauthRequired`（instanceof/kind 判定）→ 进程内标该账号 suspended(后续 tick 跳过) + `setAccountEnabled(id,false)` + 记 code+kind**（**先置进程内 suspended 标志、再 try `setAccountEnabled`——DB 写失败可容忍,本轮已停轮询、下次启动重载会再 suspend**）——不得每 tick 重列重 get 重 403 紧打 Google；**瞬时 429/一般轮询异常只记日志、保持 `enabled`、下 tick 重试(不 suspend)**；**共享信号量**全局并发上限(默认 ≤4,`// ponytail: cap≥账号数则无 tick 饿死`)；**先 acquire 信号量再取 per-account 锁**(避免排队账号持锁致 tick 误判重入；排队 tick 跳过可接受)；**单轮 poll 超时**(googleapis 无连接可 destroy、超时界定本轮+释放名额、靠 per-mail `processedAt` 保部分完成一致、`AbortSignal` best-effort；或显式 `// ponytail` 接受无界)
- [x] 5.2 单测：多账号并发；一账号抛异常(含 `ProviderReauthRequired`)其他照常；**`ProviderReauthRequired` → 该账号被 suspend(后续 tick 跳过、不再 poll)+ `enabled` 置 false**；并发不超上限；单账号不重入；挂死 poll 受超时约束、不永久占名额
- [x] 5.3 `src/main.ts`：**原子替换**现有「`loadImapAccount`→`ensureAccountAnchor`→单账号 startScheduler」为「`loadEnabledAccounts` → 每账号按 provider 构造 poller + per-account 调度」；无账号则只记日志；优雅关闭停全部调度

## 6. 账号 onboarding CLI

- [x] 6.1 `src/cli/account.ts`：`account add --imap`（host/port/tls/`--email`/**可选 `--account-id`(对齐 legacy `IMAP_ACCOUNT_ID`)** 经 flag，**口令经交互 prompt(echo off)/stdin、禁 argv** → 经 `createAccount`/`upsertAccount`：`id`=`--account-id`‖`imap:<user>@<host>`、`email` 非空、写 authJson）；`account list`（id/provider/email/enabled，**不**显示凭据）；`account disable <id>`（提示「未重启不生效，撤销账号请重启」）；同 id 已存在 → 默认拒绝并提示（或显式确认更新凭据）
- [x] 6.2 `account add --gmail`：跑 **loopback OAuth**（offline+`prompt=consent`+PKCE+state、临时端口、**禁 OOB**）→ `getProfile` 取邮箱仅小写经写入路径：`id`=`gmail:<email>`、`email`=邮箱、存 refresh token；**同 id 已存在 → upsert 新 refresh token + 置 `enabled=true`**（这是 re-auth/恢复路径——prompt=consent 可能已在 Google 侧轮换旧 token,故用新 token 覆盖、并解除 reauth-suspend,**不**拒绝；区别于 `--imap` 的 reject-on-exists）；**若被覆盖行原 `enabled=false` 则提示「正在重新启用该账号」**（enabled 重载、本期不区分「用户手动 disable」vs「reauth-suspend」,re-auth 经交互 OAuth 故非静默,见 design `// ponytail`）；**禁**回显/记录凭据明文；**文档：用 Desktop-app OAuth client（无需 GCP 预注册具体端口）；约束是授权 URL 与 `getToken` 两处 `redirect_uri` 必须含端口精确一致**
- [x] 6.3 单测：`account list` 不含凭据；`account add --imap` 建行 `id`=派生/`--account-id`、`email` 非空、凭据写 authJson、不回显、口令不取自 argv；同 id 拒绝；provider 校验

## 7. 迁移 P3 env-IMAP

- [x] 7.1 一次性迁移脚本（**直接读 config/`process.env` 的 `IMAP_*`，不依赖已在 2.3 移除的 `loadImapAccount`**；**先于 1.2 的 env 清理执行**）：经 `createAccount` 写一条 `MailAccount` 行（`provider=imap`、authJson 含 env 凭据、`email`=user@host、`MailAccount.id` = **P3 实际 accountId** `deriveAccountId(IMAP_ACCOUNT_ID, user, host)`）；迁移后从 config 删 env `IMAP_*`
- [x] 7.2 验证迁移后该账号去重键/游标与迁移前 `mail_messages.accountId` 一致（不重复处理历史）；同邮箱后续 CLI add 以同一 id upsert 命中同一行、不分裂（legacy 自定义 id 用 `--account-id` 对齐）

## 8. 端到端验证

- [x] 8.1 `pnpm test` 全绿、`pnpm build` 通过
- [x] 8.2 配 ≥2 个账号（≥1 Gmail + 1 IMAP）手测：各账号未读被分类分流；Gmail 打**权威标签**、P2/P3 去 UNREAD、P0/P1/P4 保持 UNREAD（带标签可见）；IMAP P2/P3 标 Seen、P0/P1/P4 未读；P0/P4 推送；**各账号去重/游标互不干扰**；**一账号 IMAP/OAuth 故障（含 token 撤销/scope 失配）不影响其他、该账号标记需重授权**；已处理长期未读不重复 get；大量未读分页不饿死旧件；HTML-only 有正文进模型；重启不重复处理；崩溃（markProcessed 前）后重跑不漏不重
