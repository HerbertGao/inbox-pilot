## 1. 前置决策（eval 守门机制）

- [x] 1.1 守门机制**已定案**（见 design「守门机制」节）：安全线 PR 阻塞 + paths 过滤、**k=5 多数表决（≥3/5 落 P4）**；噪音线 nightly + **Y=70%**；配 `OPENROUTER_API_KEY` secret（成本 <$0.5/月、`pull_request` + fork 无 key skip 标绿）；语料二象限矩阵（象限B 真脱敏 ≥50%、象限A ≤30% 封顶）
- [ ] 1.2 **剩余人工动作**（gate 实装）：① ⏳用户把 `OPENROUTER_API_KEY` 加为 GitHub Actions secret；② ⏳人工脱敏的象限B 真钓鱼样例 ≥6 条填入 `corpus.ts`（内容欺骗强/表层弱，不可自动生成）；③ ✅eval 骨架已搭（`src/classifier/eval/` runner+corpus+README、`pnpm eval:*`、`.github/workflows/eval.yml`、无 key skip 标绿）

## 2. Prompt 收紧

- [x] 2.1 `src/classifier/prompt.ts`：P4 收窄为「需内容层欺骗证据」，可疑 TLD/截断/未渲染模板/单独 return-path 等表层信号不单独触发 P4
- [x] 2.2 `prompt.ts`：P0 去除「交易/告警」一刀切，交易收据/例行通知 → P2/P3
- [x] 2.3 `prompt.ts`：各 P 级补反例 + 默认下沉（拿不准往低判）；保留「只输出 JSON / 禁建议自动收发 / 银行医院保险合同不轻易 P3」+ **正向保留「真钓鱼/内容欺骗仍归 P4」**（收紧触发条件、非移除 P4）
- [x] 2.4 **同步 PROJECT_INIT 的 P0/P4 旧口径**——**§11**（prompt 原文，约 :511/:518）**与 §5 优先级表**（约 :191-195，同款「疑似钓鱼/支付风险→立即推送」「P0 含交易/告警」文案）；prompt.ts 注释自承 §11 为权威原文源、CLAUDE.md 指 PROJECT_INIT 为完整需求权威——或在两处显式注记「以 email-classification spec 为准」，防文档（宽）/代码（紧）漂移

## 3. 双向回归语料（依赖任务组 1 的决策）

- [x] 3.1 建**误报**语料：网易登录 / PayPal 收据 / HKSS / 交易收据
- [ ] 3.2 建**真钓鱼**语料（按 1.1：脱敏真样例或合成钓鱼样例，禁止在仓库存真实恶意链接/PII）——**必须覆盖「表层信号弱、内容欺骗强」的边缘样例**（非对称收紧最易误伤者，见 design 决策 3），并评审语料分布、避免「教科书钓鱼」偏差
- [x] 3.3 实现 eval 运行（按定案：真调模型；真钓鱼线**逐样本 k=5 多数表决（≥3/5 落 P4 即 PASS）**、误报线 k=1 通过率阈值 Y=70%；无 key → skip 标绿）

## 4. 验证

- [ ] 4.1 真钓鱼集守门（安全线）：**逐样本 k=5 多数表决**——某样例 <3/5 落 P4 即 FAIL（区分单次抖动 vs 系统性误伤；证明非对称收紧未削弱检测）
- [ ] 4.2 误报集（噪音线）：**通过率阈值**——≥阈值降出 P0/P4 即通过（吸收 LLM 抖动；验证过度评级被压下）
- [x] 4.3 确认离线引擎兜底测仍绿（`classifyEmail.test.ts`「AI 失败/低置信→P1」不回归）；规则引擎硬底线未被本 change 触碰
