schema: spec-driven

context: |
  项目：inbox-pilot——无 GUI 的 AI 邮件路由器后台服务。
  接入 Gmail + IMAP，轮询未读邮件，用 OpenRouter 做 AI 分类，按优先级执行
  通知 / 标已读 / 打标签或移动文件夹 / 每日摘要。完整需求见 PROJECT_INIT.md。

  技术栈：
  - Runtime: Node.js 24 + TypeScript
  - DB: PostgreSQL 16 + Prisma
  - IMAP: imapflow | Gmail: googleapis
  - AI: 只通过 OpenRouter（OpenAI 兼容 SDK），禁止直连 OpenAI/Claude/Gemini
  - Scheduler: node-cron | Config: dotenv + yaml | Validation: zod | Log: pino
  - HTTP: fastify | 部署: hangar 托管的原生 node 进程（launchd daemon 加载 dist/pipeline.js）；
    docker 只剩 docker-compose.yml 里的 postgres（无 Redis）

  优先级模型：P0 立即通知不标已读 / P1 入摘要不标已读 / P2 标已读入摘要 /
  P3 静默标已读只计数 / P4 风险邮件立即通知不标已读。

  不可违反的安全约束（规则引擎 > LLM）：
  - 绝不自动发送/回复邮件。
  - LLM 只给建议，最终动作由 applySafetyRules 规则引擎决定。
  - P4 永不自动标已读。敏感邮件（银行/医院/保险/支付/合同/安全）由规则引擎内容轴落地：关键词轴确定性兜底（支付/合同/安全/医院/保险/账单类）+ 类别轴概率性广覆盖（finance/security/transaction），不依赖域名白名单；残留缺口（未命中关键词且被判非敏感类别者，如纯银行/医院/保险通知）非零、best-effort（不维护域名白名单的取舍）。
  - AI 分类失败或 confidence < 0.65 时：降级 P1、不标已读、入摘要（验证码/P4 等安全强制不被低置信度下调）。
  - 所有 provider 原始邮件先转成 NormalizedEmail 再进分类器。
  - 去重键 (accountId, providerMessageId)；重启不重复处理。
  - 通知不泄露完整正文；app 凭据（OPENROUTER_API_KEY / GMAIL_CLIENT_* 等）从环境变量读；账号凭据存 DB MailAccount.authJson、整体绝不入日志。

  范围：MVP 不做 GUI、规则学习、向量库、MCP、自动回信、附件深解析、多用户。

rules:
  proposal:
    - 始终包含「非目标」部分，明确不纳入 MVP 的能力
  specs:
    - 任何涉及标已读/发送动作的需求，必须显式声明规则引擎兜底场景
  tasks:
    - 按 PROJECT_INIT.md 的 Phase 1-5 顺序拆分，每个任务可独立验证
